# ── Unity-generated (never commit; regenerated locally) ──────────────────────
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
[Ll]ogs/
[Uu]ser[Ss]ettings/
[Mm]emoryCaptures/
# Unity crash / crash reports
sysinfo.txt
mono_crash.*.json

# ── IDE / editor ─────────────────────────────────────────────────────────────
.vs/
.idea/
*.csproj
*.sln
*.suo
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.opendb
*.VC.db
*.tmp

# ── OS ───────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Dropped-in foreign archives / dead code (not part of BasicRPG) ─────────────
# These sit in the project root but are NOT under Assets/, so Unity doesn't
# compile/use them. Excluded from version control to keep the repo clean.
/Erbium-master.zip
/unity-rpg-master.zip
/MistbornAllomancyKernel.cs
# Foreign "steel-push / iron-pull proof" + "allomantic force kernel" material re-pushed with
# download links. NOT part of BasicRPG — Iron/Steel stays one IronSteel component (no foreign
# SteelPushSystem/BurnMath kernel). Ignored so they can't be committed by accident.
/steelpush_ironpull_proof.pdf
/steelpush_ironpull_simulator.html
/allomantic_force_kernel_proof.pdf
/allomantic_force_kernel_proof (1).pdf
/GDD.zip
# Nine more foreign OSS archives dropped in as REFERENCE-ONLY (see REFERENCE_ARCHIVES.md).
# NOT under Assets/ → Unity never compiles them; never committed. Two are foreign ability-system
# frameworks that would replace the canon Allomancer, two are foreign inventories — none are deps.
/Flexi-main.zip
/Inventory-Pro-master.zip
/Inventory-master.zip
/NPBehave-master.zip
/Quest-System-Pro-master.zip
/Unity3d-Finite-State-Machine-master.zip
/awesome-opensource-unity-master.zip
/fluid-behavior-tree-develop.zip
/unity-gameplay-ability-system-main.zip